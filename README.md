# Web UI animation recordings

60 fps mp4 clips recorded headlessly against a dev instance. `<name>.mp4` is BEFORE (left) and AFTER (right) side by side; `<name>-before.mp4` and `<name>-after.mp4` are the individual clips.
